# Dependências base
python-dotenv>=1.0.0
psycopg2-binary>=2.9.9

# LangChain e processamento de documentos
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1

# Modelo de embeddings LOCAL (sentence-transformers)
# Substitui HuggingFaceEndpointEmbeddings (API online) por modelo local
sentence-transformers>=2.5.0
torch>=2.0.0

# HuggingFace (apenas para compatibilidade, não usa API online)
langchain-huggingface>=0.0.1

# Métricas e monitoramento
psutil>=5.9.0

# Utilitários
numpy>=1.24.0